export class AppBase {
  constructor(o: object);
  gameConfig;
  deviceMetric;
  defaultDimensions;
  appUrls;
  controller;
  soundController;
  viewportController;
  tracking;
  saves;
  settings;
}
