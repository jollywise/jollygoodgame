export class AppBase {
  constructor(opts: object);
  gameConfig;
  deviceMetric;
  defaultDimensions;
  appUrls;
  controller;
  soundController;
  viewportController;
  tracking;
  saves;
  settings;
}
