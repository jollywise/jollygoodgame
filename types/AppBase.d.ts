export class AppBase {
  constructor(opts: object);
  gameConfig;
  deviceMetric;
  defaultDimensions;
  appUrls;
  controller;
  soundController;
  pointerController;
  viewportController;
  tracking;
  saves;
  settings;
}
